# 头部姿态分析功能说明

## 功能概述

YOLO11姿态检测系统现已支持**抬头/低头动作识别**功能，通过分析YOLO检测的关键点来判断人物的头部姿态。

## 工作原理

### 关键点分析
系统使用以下COCO格式关键点进行分析：
- **鼻子** (索引0)
- **左眼/右眼** (索引1-2)
- **左耳/右耳** (索引3-4)
- **左肩/右肩** (索引5-6)

### 角度计算方法
1. **计算眼睛中心点**：平均左右眼坐标
2. **计算肩膀中心点**：平均左右肩坐标
3. **分析鼻子相对位置**：
   - 鼻子相对眼睛的垂直偏移
   - 归一化处理（使用头部长度）
   - 转换为角度估计

### 姿态分类
- **抬头** (`head_up`): 角度 < -10°（鼻子相对眼睛偏上）
- **低头** (`head_down`): 角度 > 15°（鼻子相对眼睛偏下）
- **正常** (`normal`): -10° ≤ 角度 ≤ 15°
- **未知** (`unknown`): 无法计算（关键点不足）

## 使用方法

### 1. 启用功能
在前端界面勾选"启用头部姿态分析（抬头/低头识别）"复选框

### 2. 检测模式
支持所有检测模式：
- ✅ 图片检测
- ✅ 视频检测
- ✅ 实时摄像头检测

### 3. 查看结果

#### 统计信息
在右侧结果面板会显示：
- **抬头人数** - 橙色标识
- **低头人数** - 红色标识
- **正常人数** - 绿色标识
- **未知人数** - 灰色标识

#### 个人详情
每个检测到的人物会显示：
- 人物编号
- 置信度
- 关键点数量
- **头部姿态**：显示状态和角度值

#### 图像标注
检测结果图像上会标注：
- 绿色文字：正常姿态
- 橙色文字：抬头
- 红色文字：低头
- 显示角度值（如："抬头 (-15.3°)"）

## API 使用

### 请求参数
```javascript
formData.append('analyze_head_pose', 'true')  // 启用头部姿态分析
```

### 响应数据结构
```json
{
  "success": true,
  "detections": [
    {
      "person_id": 0,
      "confidence": 0.95,
      "keypoints": [...],
      "head_pose": {
        "head_angle": -12.5,
        "pose": "head_up",
        "description": "抬头"
      }
    }
  ],
  "head_pose_statistics": {
    "total_persons": 3,
    "head_up_count": 1,
    "head_down_count": 1,
    "normal_count": 1,
    "unknown_count": 0,
    "head_up_percentage": 33.3,
    "head_down_percentage": 33.3
  }
}
```

## 调优参数

可在 `backend/head_pose_analyzer.py` 中调整：

```python
analyzer = HeadPoseAnalyzer(
    head_up_threshold=-10.0,    # 抬头阈值（度）
    head_down_threshold=15.0    # 低头阈值（度）
)
```

### 参数说明
- **head_up_threshold**: 负值，越小越严格（如-15表示需要更明显的抬头）
- **head_down_threshold**: 正值，越大越严格（如20表示需要更明显的低头）
- **置信度阈值**: 默认0.3，可调整关键点可信度要求

## 应用场景

### 1. 课堂监控
- 检测学生是否专注听讲
- 统计低头玩手机的学生数量
- 生成注意力报告

### 2. 办公监测
- 监测工作姿态
- 预防颈椎问题
- 提醒保持正确坐姿

### 3. 健康管理
- 检测长时间低头
- 提醒调整姿势
- 收集姿态数据

### 4. 安全监控
- 检测异常行为
- 疲劳驾驶检测
- 注意力分散识别

## 技术特点

✅ **实时分析** - 支持视频流和摄像头实时检测  
✅ **高准确度** - 基于YOLO11高精度关键点检测  
✅ **低延迟** - GPU加速，50ms检测间隔  
✅ **灵活配置** - 可调整阈值适应不同场景  
✅ **可视化** - 直观的颜色标注和统计图表  

## 注意事项

1. **关键点质量**
   - 需要清晰可见的面部关键点
   - 侧面角度可能影响准确度
   - 遮挡会导致"未知"状态

2. **摄像头角度**
   - 正面拍摄效果最佳
   - 避免极端仰角或俯角
   - 保持适当距离

3. **光照条件**
   - 充足光照提高检测质量
   - 避免逆光或强反光
   - 均匀照明最佳

4. **性能考虑**
   - 启用分析会略微增加计算量
   - GPU环境下几乎无影响
   - CPU环境下可能增加10-20ms延迟

## 文件结构

```
backend/
├── app.py                    # Flask API（集成头部姿态分析）
├── head_pose_analyzer.py     # 头部姿态分析模块
└── models/
    └── yolo11n-pose.pt      # YOLO11姿态检测模型

frontend/vuefront/src/
├── App.vue                   # 主界面（UI控件和结果显示）
└── AppStyles.css            # 样式定义
```

## 后续改进方向

- [ ] 支持多角度检测（侧面、斜面）
- [ ] 添加时间序列分析（持续低头时长）
- [ ] 支持动作识别（点头、摇头）
- [ ] 添加预警功能（长时间低头提醒）
- [ ] 导出分析报告（CSV、PDF）
- [ ] 支持批量图片/视频分析
